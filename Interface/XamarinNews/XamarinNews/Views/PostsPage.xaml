<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Title}"
             x:Class="XamarinNews.Views.PostsPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="20">
                <Label x:Name="PostTitle" LineBreakMode="WordWrap" FontSize="Title" TextColor="DarkBlue"/>
                <StackLayout Orientation="Horizontal" HeightRequest="300" >
                    <Grid>
                        <Image Source="no_image.jpg" VerticalOptions="CenterAndExpand" Aspect="AspectFit"/>
                        <Image x:Name="PostImage" VerticalOptions="CenterAndExpand" Aspect="AspectFit"/>
                    </Grid>
                </StackLayout>
                <Label x:Name="PostContent" LineBreakMode="WordWrap"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>