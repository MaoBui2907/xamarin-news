<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="{Binding Title}"
             x:Class="XamarinNews.Views.PostsPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Label x:Name="PostTitle" Text="{Binding t}" LineBreakMode="WordWrap" FontSize="Title" TextColor="DarkBlue"/>
                <StackLayout Orientation="Horizontal" HeightRequest="300" >
                    <Image x:Name="PostImage" Source="{Binding Image}" VerticalOptions="Center" Aspect="AspectFit"/>
                </StackLayout>
                <Label x:Name="PostContent" Text="{Binding Content}" LineBreakMode="WordWrap"/>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>